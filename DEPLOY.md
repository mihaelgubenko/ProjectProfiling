# 🚀 Инструкция по деплою на Railway

## Подготовка к деплою

### 1. Создание репозитория на GitHub

1. Создайте новый репозиторий на GitHub
2. Инициализируйте Git в локальной папке:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/ads-portfolio.git
git push -u origin main
```

### 2. Настройка переменных окружения

**Важно:** Не коммитьте файл `.env` или `config.js` с реальными токенами!

Перед коммитом убедитесь, что:
- ✅ `.env` добавлен в `.gitignore`
- ✅ `config.js` добавлен в `.gitignore` (если содержит токены)
- ✅ `env.example` содержит только примеры без реальных данных

### 3. Деплой на Railway

#### Шаг 1: Создание проекта

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Выберите ваш репозиторий `ads-portfolio`

#### Шаг 2: Настройка переменных окружения

1. В проекте Railway откройте вкладку "Variables"
2. Добавьте следующие переменные:

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
PORT=3000
```

**Где взять токены:**
- `TELEGRAM_BOT_TOKEN`: [@BotFather](https://t.me/BotFather) → `/newbot`
- `TELEGRAM_CHAT_ID`: [@userinfobot](https://t.me/userinfobot) → `/start`

#### Шаг 3: Деплой

1. Railway автоматически определит Node.js проект
2. Начнётся автоматическая сборка и деплой
3. После завершения вы получите URL вида: `https://your-project.railway.app`

#### Шаг 4: Настройка домена (опционально)

1. В настройках проекта Railway откройте "Settings"
2. В разделе "Domains" добавьте свой домен
3. Следуйте инструкциям для настройки DNS

### 4. Проверка работы

1. Откройте ваш сайт по URL от Railway
2. Заполните тестовую форму
3. Проверьте, что уведомление пришло в Telegram

## Обновление сайта

После внесения изменений:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

Railway автоматически перезапустит приложение с новыми изменениями.

## Устранение проблем

### Ошибка "Cannot find module"

Убедитесь, что все зависимости указаны в `package.json`:
```bash
npm install --save express axios cors dotenv
```

### Формы не отправляются

1. Проверьте переменные окружения в Railway
2. Проверьте логи в Railway Dashboard
3. Убедитесь, что токен Telegram бота действителен

### Порт не работает

Railway автоматически устанавливает переменную `PORT`. Убедитесь, что в `server.js` используется:
```javascript
const PORT = process.env.PORT || 3000;
```

## Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Node.js Best Practices](https://github.com/goldbergyoni/nodebestpractices)

