# 📦 Первоначальная настройка Git

## Быстрый старт

### 1. Инициализация репозитория

```bash
git init
```

### 2. Добавление всех файлов

```bash
git add .
```

### 3. Первый коммит

```bash
git commit -m "Initial commit: Portfolio website with retro newspaper design"
```

### 4. Создание репозитория на GitHub

1. Зайдите на [github.com](https://github.com)
2. Создайте новый репозиторий (например, `ads-portfolio`)
3. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

### 5. Подключение к удалённому репозиторию

```bash
git branch -M main
git remote add origin https://github.com/ваш-username/ads-portfolio.git
git push -u origin main
```

## ⚠️ Важно перед коммитом

Убедитесь, что файл `.env` **НЕ** добавлен в репозиторий:

```bash
# Проверьте, что .env в .gitignore
cat .gitignore | grep .env

# Если файл уже отслеживается, удалите его из индекса
git rm --cached .env
```

## 📝 Рекомендуемые файлы для коммита

✅ **Добавить:**
- Все исходные файлы (HTML, CSS, JS)
- `package.json`, `package-lock.json`
- `README.md`, `LICENSE`
- `Procfile`, `railway.json`
- `env.example` (без реальных токенов!)
- Папка `images/`

❌ **НЕ добавлять:**
- `.env` (содержит токены)
- `node_modules/`
- `config.js` (если содержит токены - используйте переменные окружения)
- Логи и временные файлы

## 🔄 Рабочий процесс

### Ежедневная работа

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Закоммитить
git commit -m "Описание изменений"

# Отправить на GitHub
git push origin main
```

### Создание новой ветки для функции

```bash
git checkout -b feature/new-feature
# ... внести изменения ...
git add .
git commit -m "Add new feature"
git push origin feature/new-feature
```

## 📋 Полезные команды

```bash
# Просмотр истории коммитов
git log --oneline

# Отмена последнего коммита (сохраняя изменения)
git reset --soft HEAD~1

# Просмотр изменений
git diff

# Просмотр удалённых репозиториев
git remote -v
```

